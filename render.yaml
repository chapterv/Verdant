services:
  - type: web
    name: web-agent-green
    env: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python run.py --config config/config.yaml --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
